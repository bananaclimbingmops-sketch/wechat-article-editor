# Gemini → Codex 迁移基线

> 状态：设计确认前  
> 目标仓库：`/Users/mac/Documents/ChatGPT/微信公众号编辑器`

## 1. 已掌握的源资料

- `k11.tsx`：已有 React 手机文章预览，包含 K11 文章的主要视觉与内容结构。
- 6 张 K11 实拍图片：将作为首个示例文章素材迁入项目。
- 公众号编辑器相关代码：11 个文章 HTML 片段，用于提取可复用的微信排版模式。
- 《复现微信公众号编辑器核心技术》PDF：提供微信编辑器兼容策略。
- `wechatskill.md`：实际为 RTF 格式，内容可作参考，但不直接作为可执行 Markdown 规范。
- Gemini 分享链接：只作为背景上下文；本地代码和文档是迁移事实来源。

## 2. 迁移目标

把现有“单篇文章预览”升级为一个可运行、可扩展、可验证的公众号文章工作台：

1. 保留并复现 K11 示例文章。
2. 支持导入或粘贴文章原文及关联图片。
3. 以结构化内容块编辑文章，而不是直接修改一整段脆弱 HTML。
4. 实时提供 React 手机预览。
5. 单独生成微信兼容的内联 HTML。
6. 在导出前验证原文、图片与 HTML 兼容性。

## 3. 技术边界

### 3.1 第一版采用

- Vite
- React
- TypeScript
- CSS Modules 或普通分层 CSS（不依赖 Tailwind 才能运行）
- Vitest + Testing Library
- 浏览器本地存储用于草稿自动保存

图标优先使用 `lucide-react`。应用 UI 可以使用现代 CSS Grid/Flex；微信导出 HTML 不继承这些实现。

### 3.2 第一版暂不包含

- 登录、团队协作与云同步
- 公众号 API 授权与直接发布
- 后端数据库
- 任意网页的像素级抓取
- 类 Word 的无限制富文本能力
- 自动改写、缩写或润色原文

这些能力可以后续增加，但不能污染第一版的内容一致性和导出稳定性。

## 4. 目标架构

```text
src/
  app/                    # 应用壳、路由和全局状态
  domain/
    article.ts            # 文章、段落、图片、组件等领域模型
    validation.ts         # 校验结果模型
  examples/
    k11/                  # K11 结构化文章数据与元信息
  features/
    import/               # 原文、图片和示例导入
    outline/              # 大纲和内容块排序
    editor/               # 结构化编辑器
    preview/              # React 手机预览
    export/               # 微信兼容 HTML 生成器
    validation/           # 原文、素材和兼容性校验
  components/             # 应用级通用 UI 组件
  styles/                 # DESIGN.md 对应的 tokens 与全局样式
public/
  assets/k11/             # K11 原始图片的项目副本
tests/
  fixtures/               # 原文和 HTML 回归样本
```

### 4.1 单一内容源

文章以结构化数据作为唯一事实来源：

```ts
type ArticleDocument = {
  id: string;
  title: string;
  sourceText: string;
  blocks: ArticleBlock[];
  assets: ArticleAsset[];
  metadata: ArticleMetadata;
};
```

- `sourceText` 保存导入原文，永不被排版逻辑覆盖。
- `blocks` 保存编辑后的结构与展示意图。
- React 预览从 `blocks` 渲染。
- 微信 HTML 生成器同样从 `blocks` 渲染，但使用独立的安全标签与内联样式白名单。
- 校验器比较 `sourceText` 与内容块中的可见文字，报告差异但不自动修正。

### 4.2 双渲染器

| 渲染器 | 用途 | 允许能力 |
| --- | --- | --- |
| React Preview Renderer | 编辑过程中的快速视觉反馈 | React 组件、应用 CSS、现代布局 |
| WeChat HTML Renderer | 复制到微信公众号编辑器 | 内联样式、稳定标签、受控嵌套、无脚本 |

双渲染器共享文章模型和品牌设计 token，不共享 DOM 实现。这样能防止把 Tailwind、React 属性或复杂布局误导出到微信。

## 5. 微信 HTML 兼容策略

- 所有最终样式内联。
- 可继续回车编辑的文字以 `<p style="margin:0;...">` 为基本单元。
- 避免外部 CSS、脚本、动画和依赖伪元素才能理解的内容。
- 复杂横向排列只在必要时使用简单 `<table>`；正文优先自然块级流。
- 图片输出 `display:block; width:100%; max-width:100%; height:auto;`，并保留 alt。
- 导出时清除应用 class、事件属性和 React 专有属性。
- 对非法嵌套、缺失图片、Markdown 残留符号和不支持样式给出显式警告。

## 6. 校验体系

### 6.1 原文一致性

- 保存原始输入的不可变快照与 SHA-256 指纹。
- 提取内容块全部可见文字，在只规范换行和 Unicode 空白的前提下比较。
- 发现新增、缺失、调序时给出块级差异，不自动修改。
- `**文字**` 等 Markdown 痕迹应在解析阶段转为强调语义，不能作为星号显示；可见文字仍保持一致。

### 6.2 素材完整性

- 每个图片块都必须引用存在的素材 ID。
- 校验文件类型、自然尺寸、替代文本与预览加载状态。
- 导出前列出尚未转换为可粘贴/可上传资源的本地图片。

### 6.3 HTML 兼容性

- 禁止 `<script>`、外部 `<link>`、事件处理属性和 `javascript:` URL。
- 警告 `class`、复杂 Flex/Grid、position fixed/sticky、CSS 动画和过深嵌套。
- 检查 `<p>` 的合法父级关系、空段落策略、图片宽度和内联样式。
- 使用固定 K11 快照做结构与文本回归测试。

## 7. 实施阶段

### 阶段 0：设计确认

- 确认 `DESIGN.md` 的信息架构、视觉方向、L1 交互等级与第一版范围。

### 阶段 1：工程与示例迁入

- 初始化 Vite React TypeScript 工程。
- 建立设计 token、基础布局与测试基线。
- 复制 6 张 K11 图片到 `public/assets/k11/`。
- 将 `k11.tsx` 拆为结构化文章数据与 React 预览组件。
- 修复 Markdown 星号直接显示、根路径依赖和滚动条样式缺口。

### 阶段 2：工作台核心

- 实现顶栏、文章大纲、内容块编辑器和右侧检查器。
- 支持段落、标题、图片、引语、数据卡、提示卡等首批内容块。
- 支持块排序、复制、删除、撤销与本地自动保存。

### 阶段 3：微信导出与校验

- 实现微信 HTML 生成器。
- 实现原文一致性、图片完整性和 HTML 兼容性校验。
- 实现复制 HTML、下载 HTML 和导出预览。

### 阶段 4：质量验证

- 完成桌面、平板、移动布局检查。
- 完成键盘操作、焦点、对比度和减少动态效果检查。
- 对 K11 示例进行文本、视觉、资源和导出回归。
- 在真实微信公众号编辑器中进行最终粘贴验证；此步骤需要用户已登录的浏览器会话或用户手动配合。

## 8. 完成标准

- `npm install && npm run dev` 能启动编辑器。
- K11 示例的文字、图片、结构和主要品牌视觉完整呈现。
- 用户可编辑内容块并在手机预览中立即看到结果。
- 原文校验可定位缺失、增加和调序问题。
- 微信 HTML 无脚本、无外部样式依赖、无 React/Tailwind 残留。
- 用户可一键复制或下载导出 HTML。
- 关键逻辑测试通过，生产构建无 TypeScript 错误。
- 不同视口下界面可用，键盘焦点清楚。

## 9. 当前迁移决策

| 决策 | 当前选择 | 原因 |
| --- | --- | --- |
| 交互等级 | L1 精致静态 | 编辑器应优先稳定、效率与清晰反馈 |
| 数据存储 | 浏览器本地 | 最快形成完整可用闭环，后续可替换为云端 |
| 内容模型 | 结构化块 | 支持双渲染、校验和组件化排版 |
| UI 与导出 | 双渲染器 | 微信兼容约束与现代应用 UI 约束不同 |
| 首个回归样本 | K11 | 现有内容、图片与视觉资料最完整 |
| 发布方式 | 复制/下载 HTML | 暂不扩大到公众号授权和远程发布 |


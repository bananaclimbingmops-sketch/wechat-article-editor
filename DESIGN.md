# 微信公众号文章工作台 — 设计规范

> 版本：0.1（迁移基线）  
> 状态：待确认  
> 交互等级：L1 精致静态交互  
> 目标：把文章内容、品牌组件、手机预览和微信兼容 HTML 导出整合为一个可靠的本地工作台。

## 1. 设计方向

### 1.1 产品定位

这是一个面向公众号编辑流程的生产力工具，不是通用网页搭建器。它要同时解决三件事：

1. 原文无损：导入后不擅自缩写、改写或遗漏任何文字。
2. 视觉可控：用品牌化组件快速完成专业排版，并实时查看手机阅读效果。
3. 微信可用：导出以内联样式、稳定标签和可编辑段落为核心的 HTML。

### 1.2 视觉关键词

- 温暖、可靠、轻快、清晰、手作感。
- “温暖的专业工作台”作为骨架，“攀岩品牌的活力”作为点缀。
- 以奶油白工作区、深棕文字、香蕉黄主操作、橙色提醒、青绿色辅助状态构成识别度。
- 阴影偏硬、边框清晰、圆角克制；不做玻璃拟态、霓虹渐变或大面积装饰。

### 1.3 核心体验原则

- 编辑、预览、校验始终处于同一视野，减少来回切换。
- 所有影响文章内容的操作都应可见、可撤销、可验证。
- React 预览层服务于编辑体验；微信 HTML 导出层单独实现，不复用不稳定的应用样式。
- 重要动作使用明确文案，例如“复制微信 HTML”，不只依赖图标。

## 2. 信息架构与页面布局

### 2.1 桌面端主结构（≥ 1280px）

应用采用固定顶栏与三栏工作区：

| 区域 | 建议宽度 | 内容 |
| --- | ---: | --- |
| 顶栏 | 100% × 64px | 品牌、文章标题、保存状态、导入、校验、导出主操作 |
| 左侧栏 | 272px | 文章信息、内容大纲、组件库、素材入口 |
| 中央画布 | minmax(560px, 1fr) | 结构化内容块编辑、排序、插入与状态提示 |
| 右侧检查器 | 420px | 手机预览、微信 HTML、校验结果三个页签 |

整体工作区最小高度为 `calc(100vh - 64px)`。左、右栏固定在视口内独立滚动；中央画布保留最多 32px 外边距，内容编辑列最大宽度 780px。

### 2.2 页面层级

1. 工作台
   - 文章大纲
   - 内容块编辑
   - 手机预览
   - HTML 检查
   - 一致性校验
2. 导入面板
   - 粘贴原文
   - 载入示例
   - 导入图片
3. 导出面板
   - 复制微信 HTML
   - 下载 HTML
   - 查看导出注意事项

### 2.3 关键流程

`导入原文与图片 → 识别文章结构 → 编辑内容块 → 手机预览 → 原文/图片/兼容性校验 → 复制或下载微信 HTML`

任何校验失败都不删除用户内容。导出按钮仍可见，但以警告状态说明风险，并提供“返回定位问题”。

## 3. 色彩系统

所有应用颜色必须来自 CSS 变量。透明色使用对应 RGB 变量，不手写散落的 rgba 数值。

```css
:root {
  --color-brand-yellow: #ffda2a;
  --color-brand-yellow-rgb: 255, 218, 42;
  --color-brand-brown: #895421;
  --color-brand-brown-rgb: 137, 84, 33;
  --color-brand-orange: #f98435;
  --color-brand-orange-rgb: 249, 132, 53;
  --color-brand-cream: #fff6e0;
  --color-brand-cream-rgb: 255, 246, 224;
  --color-brand-teal: #84ccc9;
  --color-brand-teal-rgb: 132, 204, 201;

  --color-bg-app: #f4f1ea;
  --color-bg-app-rgb: 244, 241, 234;
  --color-bg-panel: #ffffff;
  --color-bg-panel-rgb: 255, 255, 255;
  --color-bg-subtle: #faf8f3;
  --color-bg-subtle-rgb: 250, 248, 243;

  --color-text-primary: #2d241c;
  --color-text-primary-rgb: 45, 36, 28;
  --color-text-secondary: #6f655b;
  --color-text-secondary-rgb: 111, 101, 91;
  --color-text-tertiary: #9a9087;
  --color-text-tertiary-rgb: 154, 144, 135;
  --color-border: #e5dfd5;
  --color-border-rgb: 229, 223, 213;
  --color-border-strong: #cfc5b7;
  --color-border-strong-rgb: 207, 197, 183;

  --color-success: #2f8a66;
  --color-success-rgb: 47, 138, 102;
  --color-warning: #d86f28;
  --color-warning-rgb: 216, 111, 40;
  --color-danger: #c94747;
  --color-danger-rgb: 201, 71, 71;
  --color-focus: #6c4a25;
  --color-focus-rgb: 108, 74, 37;
}
```

### 3.1 使用规则

- 香蕉黄只用于主按钮、选中态、品牌标记和极少量强调，避免整屏发黄。
- 深棕承担正文、标题和聚焦边框，保证暖色体系中的可读性。
- 橙色用于有风险但可继续的状态；红色只用于真正的错误或内容缺失。
- 青绿色用于成功状态、图片素材和辅助提示，不与主操作竞争。
- 应用背景与文章预览背景分离：工作台为暖灰，手机文章默认白底。

## 4. 字体与排版

### 4.1 字体栈

```css
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@500;600;700&family=JetBrains+Mono:wght@400;500&family=Noto+Sans+SC:wght@400;500;600;700;800&display=swap');

:root {
  --font-sans: 'Noto Sans SC', 'PingFang SC', 'Microsoft YaHei', sans-serif;
  --font-ui-latin: 'Inter', 'Noto Sans SC', sans-serif;
  --font-mono: 'JetBrains Mono', 'SFMono-Regular', Consolas, monospace;
}
```

离线或网络不可用时必须自然回退到系统中文字体，不阻塞页面渲染。

### 4.2 字号层级

| 用途 | 字号 / 行高 | 字重 | 说明 |
| --- | --- | --- | --- |
| 页面标题 | 24 / 32px | 700 | 顶栏文章名与主要面板标题 |
| 面板标题 | 18 / 26px | 700 | 左右栏一级分区 |
| 卡片标题 | 16 / 24px | 600 | 内容块名称、校验项 |
| 应用正文 | 15 / 24px | 400 | 控件说明与元数据 |
| 编辑正文 | 16 / 29px | 400 | 长文编辑区，行高约 1.8 |
| 辅助文本 | 13 / 20px | 400–500 | 状态、时间、计数 |
| 代码文本 | 13 / 21px | 400 | HTML 查看器 |

- 中文正文最小字距为 `0.02em`；长文内容不低于 16px。
- 标题不使用全大写；英文短标签可用 0.06em 字距。
- 装饰效果只允许用于一个品牌徽标或示例文章封面标题，可采用 2px 深棕硬阴影；正文与控件文字禁用阴影、描边、渐变填充。

## 5. 间距、尺寸与视觉骨架

### 5.1 基础变量

```css
:root {
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-5: 20px;
  --space-6: 24px;
  --space-8: 32px;
  --space-10: 40px;
  --space-12: 48px;

  --radius-sm: 8px;
  --radius-md: 12px;
  --radius-lg: 18px;
  --radius-pill: 999px;

  --shadow-card: 0 1px 2px rgba(var(--color-text-primary-rgb), 0.06),
                 0 8px 24px rgba(var(--color-text-primary-rgb), 0.05);
  --shadow-float: 4px 5px 0 rgba(var(--color-brand-brown-rgb), 0.16);
  --focus-ring: 0 0 0 3px rgba(var(--color-focus-rgb), 0.18);
}
```

### 5.2 密度与对齐

- 主控件高度 40px，小控件 32px，触屏下最小点击区域 44px。
- 面板内边距 20–24px，卡片间距 12–16px，内容块内边距 20px。
- 所有文本输入、按钮与页签按 4px 基线对齐。
- 手机预览画布宽 390px，圆角 28px，内部文章区域按真实移动端宽度计算。
- 文章预览不继承工作台阴影、背景和 UI 字体变量，避免误导导出结果。

## 6. 组件与状态规范

### 6.1 按钮

**主按钮**：香蕉黄背景、深棕文字、1px 深棕透明边框。

- Default：`--color-brand-yellow` 背景。
- Hover：背景加深约 6%，上移 1px，出现轻硬阴影。
- Active：回到原位，阴影缩小。
- Focus-visible：显示 `--focus-ring`，不可只依赖颜色变化。
- Disabled：45% 不透明度、无阴影、无位移、`cursor: not-allowed`。

**次按钮**：白底、标准边框、主文字；Hover 使用浅奶油背景。  
**幽灵按钮**：透明底；Hover 使用 `rgba(var(--color-brand-brown-rgb), 0.06)`。  
**危险按钮**：仅确认删除内容块时出现，默认白底红字，确认态才使用红底白字。

### 6.2 页签

- 三个固定页签：手机预览、微信 HTML、校验结果。
- Default：次级文字、透明底。
- Hover：主文字、浅背景。
- Active：深棕文字、香蕉黄下划线或底部 3px 标记。
- Focus-visible：完整圆角焦点环。
- 页签标签旁允许显示数量徽标，例如“校验结果 2”。

### 6.3 内容块卡片

- Default：白底、1px 标准边框、12px 圆角。
- Hover：边框加深，卡片上移 2px。
- Selected：深棕 2px 边框，左侧 4px 香蕉黄标记。
- Editing：编辑域白底，工具条固定在卡片顶部。
- Dragging：95% 不透明度、浮层阴影；原位置保留占位框。
- Invalid：右上角橙色/红色状态徽标，卡片底部给出可操作说明。
- Disabled/Locked：浅灰底、内容可复制但不可编辑。

### 6.4 编辑控件

- 文本域默认无内层重阴影，使用清晰边框与高行高。
- Focus：边框变为深棕并显示焦点环。
- Placeholder：使用三级文字色，不作为真正内容。
- 字数状态：正常为次级文字；检测到与原文不一致时变橙并提供差异入口。
- 段落换行在结构模型中保存为独立段落；导出时生成 `<p style="margin:0;...">`，不把可编辑正文直接挂在外层装饰容器上。

### 6.5 图片素材卡

- 默认显示缩略图、文件名、尺寸、使用位置。
- Hover 显示“替换”“定位”动作。
- Missing 显示虚线橙框和“重新关联图片”。
- Loading 使用固定比例骨架屏，避免布局跳动。
- Error 不隐藏原引用信息，保留重新上传入口。

### 6.6 状态徽标与校验项

- Success：青绿色浅底 + 对勾图标 + 明文“原文一致”。
- Warning：橙色浅底 + 感叹号 + 风险说明。
- Error：红色浅底 + 错误图标 + 定位按钮。
- Pending：中性浅底 + 进度指示，但 L1 不使用循环旋转以外的复杂动效。
- 不能只用红/绿区分，必须同时使用图标和文字。

### 6.7 手机预览框

- 外框为深棕黑，内部为 390px 文章视口。
- 工具栏提供 100%、适应高度、刷新三个动作。
- 预览内容由结构化文章模型渲染；不得直接把微信导出 HTML 注入应用 DOM 作为唯一预览实现。
- 提供“导出预览”开关，用隔离容器显示最终内联 HTML，以便对比编辑预览和真实导出。

### 6.8 HTML 查看器

- 使用等宽字体、行号、自动换行开关和复制按钮。
- 只读状态仍允许选中文本。
- 搜索命中用香蕉黄透明底，不直接改变代码文本颜色。
- 发现 `class`、外部样式表、脚本或不稳定标签时，在对应行显示警告标记。

### 6.9 文章固定开头版式

- 所有文章的标题使用香蕉黄底板、约 20–22px 圆角和向右下偏移的深棕硬阴影，不再使用白底裸标题。
- 标题底板居中，占手机正文宽度约 78–82%，允许约 `-1.2deg` 的轻微倾斜以保留品牌手作感。
- 标题优先尊重作者输入的换行；当某一行超过容器宽度时必须安全折行，禁止使用会造成溢出的强制 `nowrap`。
- 标题后的第一段正文固定使用奶油色底、香蕉黄 1px 边框和 15–16px 圆角的引言卡片。
- 当开头承担促销传播任务时，引言卡片采用“叙述引导 → 香蕉黄促销焦点 → 叙述收束 → 脚注”的四层结构；焦点区依次展示日期、适用产品和核心优惠，核心优惠字号不低于 20px，避免促销信息埋在长段落中。
- 促销焦点区保持静态、居中且最多三层文字，不使用动画、渐变或并排复杂布局；所有文字仍以独立 `<p>` 输出，确保微信后台按回车编辑时结构稳定。
- 引言卡片可包含一行较小的解释脚注；正文和脚注必须各自输出为合法 `<p style="margin:0;...">`，不可把 `<p>` 放进标题标签。
- React 编辑预览和微信 HTML 导出必须同步实现该版式，不能只修改其中一层。

### 6.10 文章末尾横滑报名入口

- 多门店报名图使用原生横向滚动容器，首张卡片占可视宽度约 84%，右侧露出下一张以提示可继续滑动。
- 横滑区保留可见滚动条，并在下方显示“左右滑动，选择报名门店”的文字提示；不使用自动轮播、脚本手势或隐藏式交互。
- 微信导出层使用 `overflow-x:auto`、`white-space:nowrap` 与 `inline-block` 卡片实现，禁止使用 Flex/Grid；每张二维码图片必须保持完整比例、不得裁切或覆盖文字。

## 7. 交互与动效

### 7.1 等级与依赖

- 当前等级：L1 精致静态交互。
- 技术：CSS transition + React 状态；不引入 GSAP 或其他动画运行时。
- 目标：让编辑行为清楚、反馈及时，不让动画干扰长文工作。

### 7.2 动效参数

```css
:root {
  --duration-fast: 120ms;
  --duration-base: 180ms;
  --duration-slow: 260ms;
  --ease-standard: cubic-bezier(0.2, 0, 0, 1);
  --ease-enter: cubic-bezier(0.16, 1, 0.3, 1);
}
```

- 页面首次进入：顶栏与三栏在 260ms 内由 `opacity: 0`、`translateY(12px)` 进入，最多错开 50ms。
- 卡片 Hover：`translateY(-2px)` + 阴影变化，180ms。
- 按钮 Hover/Active：位移不超过 1px，120ms。
- 面板切换：仅透明度与 8px 位移，180ms，不做横向大滑屏。
- 导出成功：按钮文案短暂变为“已复制”，使用颜色和图标反馈，不弹出遮挡内容的庆祝动画。

### 7.3 减少动态效果

```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    scroll-behavior: auto !important;
    transition-duration: 0.01ms !important;
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
  }
}
```

键盘操作与屏幕阅读器状态反馈不依赖动效完成。

## 8. 响应式与无障碍

### 8.1 桌面端（≥ 1280px）

- 保持三栏同时可见。
- 中央编辑列优先获得弹性空间；右侧预览不低于 390px。
- 当宽度不足时允许折叠左侧栏为 56px 图标轨道。

### 8.2 平板端（768–1279px）

- 顶栏保留主导出动作，次要动作收入“更多”。
- 左侧栏改为抽屉；中央编辑与右侧检查器以“编辑 / 预览”双页签切换。
- 不在狭窄视口中强行缩小手机预览；允许水平居中并按比例最大缩放至 90%。

### 8.3 移动端（< 768px）

- 定位为审阅与轻编辑模式，而非完整桌面排版替代。
- 顶栏 56px；底部固定四个入口：大纲、编辑、预览、校验。
- 内容单列显示，所有可点区域至少 44×44px。
- 导出放入明确的底部操作面板，并在复制后给出可读状态。

### 8.4 无障碍基线

- 正文和关键控件满足 WCAG AA 对比度。
- 所有图标按钮有 `aria-label` 和可见 Tooltip。
- Tab 顺序遵循顶栏 → 左栏 → 编辑画布 → 右侧检查器。
- 拖拽排序必须同时提供键盘“上移/下移”。
- 错误信息通过 `aria-live="polite"` 宣告，并与具体控件关联。
- 焦点样式不可移除；不能只使用颜色表达状态。

## 9. 微信导出边界与 Do / Don't

### 9.1 导出层硬规则

- 最终文章 HTML 使用内联 CSS；不依赖应用的 class、外部 CSS、脚本或字体加载。
- 重点提示标题前的装饰图标使用随文字一同输出的单色 Unicode 符号，不导出 Lucide SVG；预览层与微信导出层必须使用相同符号。
- 长文本和用户按回车生成的行使用独立 `<p style="margin:0;...">`。
- 布局优先使用 `<section>`、`<p>`、`<span>`、`<img>` 与必要的 `<table>`，避免脆弱深层嵌套。
- 不把 `<p>` 放进 `<h1>` 等非法结构；标题需要可编辑时，使用合法块结构表达。
- 图片必须有明确宽度约束、`display:block`、安全的 `height:auto` 与替代文本。
- 导出前运行原文一致性、图片完整性、标签合法性和不稳定样式扫描。

### 9.2 Do

1. 保持原文内容与段落顺序，任何自动处理都可审计。
2. 让编辑态预览与最终导出预览可以并排或快速切换对比。
3. 使用统一设计变量构建应用 UI，使用独立白名单生成微信 HTML。
4. 用清晰状态告诉用户“已保存”“原文一致”“可安全复制”。
5. 给关键动作同时提供文字、图标、键盘焦点和禁用原因。
6. 为图片保留来源文件名、尺寸和文章位置映射。
7. 将品牌风格封装为有限、可组合的文章组件。
8. 在桌面端优先保证长文编辑效率，在移动端优先保证审阅。
9. 为每种内容块提供稳定的空状态、错误状态和只读状态。
10. 以 K11 示例作为首个回归样本，持续验证预览与导出。

### 9.3 Don't

1. 不要为了“更像宣传页”牺牲编辑器的信息密度和可扫描性。
2. 不要使用大面积渐变、玻璃拟态、发光边框或持续背景动画。
3. 不要把手机预览组件本身当作可直接粘贴到微信的 HTML。
4. 不要让自动排版改写、缩写、润色或删除原文。
5. 不要在导出 HTML 中留下 Tailwind class、React 属性、脚本或外部样式依赖。
6. 不要用 `<div><br></div>` 作为主要段落模型，也不要把可编辑文字直接散落在装饰容器中。
7. 不要只以颜色区分成功、警告和错误。
8. 不要把所有操作藏进图标或右键菜单。
9. 不要在每张卡片上使用同等强度的阴影和品牌色。
10. 不要在移动端压缩成三栏缩略图；应改为清晰的单任务页签。

---

## 待确认项

1. 采用 L1 精致静态交互，不引入复杂动效库。
2. 第一版为本地单人工作台，不含登录、云同步和公众号 API 直接发布。
3. 第一版主流程为“导入/粘贴 → 结构化编辑 → 预览 → 校验 → 复制/下载 HTML”。
4. K11 文章作为内置示例与视觉回归基准。
5. 视觉方向采用“暖色专业工作台 + 香蕉攀岩品牌点缀”。
